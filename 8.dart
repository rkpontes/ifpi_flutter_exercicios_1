// Crie uma lista de strings com os nomes de três cores. Use um loop for para imprimir cada cor
// na lista.

void main() {
  List<String> cores = ["Azul", "Vermelho", "Verde"];

  for (String cor in cores) {
    print(cor);
  }
}
