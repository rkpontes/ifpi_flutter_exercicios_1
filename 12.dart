// Declare uma variável const chamada maxIdade com valor 100 e use-a para verificar se
// idade é menor que maxIdade.

void main() {
  const int maxIdade = 100;
  int idade = 30;
  if (idade < maxIdade) {
    print("$idade é menor que $maxIdade");
  }
}
